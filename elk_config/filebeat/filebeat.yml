filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /logs/log.log
    tags: ["backend"]

  - type: log
    enabled: true
    paths:
      - /logs/legacy/log.log
    tags: ["legacyApp"]

#output:
#  logstash:
#    hosts: ["logstash:5044"]

output.console:
  pretty: true